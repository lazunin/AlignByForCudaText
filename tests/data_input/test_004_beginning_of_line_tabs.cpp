else if (value.ToString().Length == 5)
{
// Мой дядя самых честных правил
}// Говна накодил впопыхах,
else// Но после сказочно разбавил
{// Его комментами в стихах.
	throw new ArgumentException();// Улучшить попросили? Нате!
	return !true && !false;// Фсем чмоке, кстати, в этом чате :))️
}